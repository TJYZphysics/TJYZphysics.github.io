# 光路塔防完整修复与重平衡

## 总结

- 以最新 `规则手册.md` 为唯一玩法基准，补齐缺失仪器、修复光路和战斗漏洞，并同步修订规则手册。
- 保留 19 关全部开放、现有地图布局和自由实验模式；重新设计战斗数值，使状态、护盾、抗性和仪器定位有足够观察时间。
- 新手引导采用不锁操作的分步实战教学；详细规则改为分页手册，不持续遮挡战场。
- 继续沿用 React 管理 UI、独立 TypeScript 模拟、Phaser 负责渲染的现有架构。

## 核心规则与接口

- 新增 `DeviceKind = 'prism-splitter'`，名称“棱镜分束器”：
  - 输入含至少两个有效 RGB 通道时自动输出 R/G/B 三路，各路功率等于原输入对应通道功率乘设备传输率。
  - 单色输入时按普通分束器工作，允许配置 2/3 路比例。
  - 三个输出均可独立吸附目标；未吸附输出按设备方向发射。
- 删除全部聚焦遗留：
  - 从 `BeamSegment`、`OpticalNetwork`、攻击范围、伤害结算、设备面板、渲染和测试中删除 `focusStrength`、`focusedInputs`、聚焦增伤等字段。
  - 从规则手册删除“聚焦光束攻击带状态目标”。
- 扩展状态和设备数据：
  - `EnemyStatus` 增加白光易伤、毒素点燃/冷热冲击冷却、辐射衰减计时。
  - `DevicePlacement` 增加范围攻击冷却和收集器输出颜色。
  - `BattleState` 增加固定步长余数与峰值功率利用率。
  - 存档升级至 v3，增加教学完成/关闭状态，并无损迁移 v1/v2 的星级和设置。
- 新增 `advanceBattle(state, level, seconds)`，以 1/30 秒固定子步推进并保留余数；1×/2×/3×、20–144Hz 下不得丢失模拟时间。

## 战斗与数值设计

- 光谱基础伤害采用 `0.060R + 0.018G + 0.025B`；裸光束仅使用 22% 伤害和 25% 状态强度，攻击终端承担主要输出。
- 光谱判定要求有效通道至少占总功率 10%，避免微量杂色误判白光：
  - 黄光附加燃烧；橙光基础伤害 ×1.25 并强化燃烧。
  - 紫光基础伤害 ×1.25、辐射积累 ×1.25。
  - 青光冻结减速；白光负责破盾。
- 状态结算：
  - 中毒 4 秒、2.2 DPS；燃烧 4 秒、3 DPS；冻结 1.8 秒、速度 ×0.55。
  - 毒素点燃消耗中毒，造成 `min(14, 6 + 2 × 剩余中毒秒数)`，冷却 1 秒。
  - 冷热冲击造成 12 点伤害、消耗冻结，冷却 1.2 秒。
  - 冻结目标受到攻击时获得 2 秒破甲。
  - 辐射 4 层触发 18 点暴击并清空；停止受蓝光 1.5 秒后以每秒 0.6 层衰减。
- 护盾改为减伤机制：
  - 重甲盾值 `max(30, 12% HP)`，Boss 为 `max(160, 18% HP)`。
  - 有盾时非白光生命伤害仅保留 35%，且不能有效消耗护盾。
  - 白光造成 `原始伤害 ×2.5` 的破盾量；破盾后施加 4 秒易伤，所有伤害 ×1.25。
  - 重甲未破甲时生命伤害另乘 0.45；元素抗性通道只承受 30% 伤害。
- 终端参数：
  - 灯泡：125 范围、全体连续攻击、光谱伤害 ×0.85。
  - 激光：300 范围、单体连续攻击、光谱伤害 ×1.65。
  - 辐射源：150 范围、全体连续攻击，将输入功率转为蓝光，伤害 ×0.55。
  - 寒冰：360° 全体脉冲，间隔 1.25 秒，伤害 `1.5 + 0.04W`，固定冻结。
  - 火焰：360° 全体脉冲，间隔 1.10 秒，伤害 `2 + 0.05W`，固定燃烧。
  - 寒冰和火焰升级后伤害每级 +20%，攻击间隔分别乘 0.86/0.74，范围每级 +8%。
  - 粒子加速器：输入至少 90W；充满后沿设备方向穿透 20px 宽、360px 长直线内全部敌人，伤害 `20 + 0.16 × 已消耗焦耳`。
  - 储能电容：最大 450J，伤害 `15 + 0.18J`，半径 `90 + 330√充能比例`，引爆后销毁。
- 范围攻击遵循“太阳花式”全范围周期命中，不做目标数递减；通过攻击周期、范围和单次伤害控制群攻价值。
- 光路守恒：
  - 镜面、分束器、棱镜分束器、合束器、滤光片、光闸传输率为 92%/96%/100%。
  - 所有分束比例在模拟边界统一归一化，不信任 UI 或存档数据。
  - 收集器 L1/L2/L3 回收 10%/15%/20%，多台总回收率封顶 30%；回收功率必须从原范围终端可用输入中扣除。
  - 收集器可指定红、绿或蓝输出，默认红色；网络断言总输出与消耗不超过输入。
- 设备价格调整为：镜面 18、分束器 32、棱镜分束器 46、合束器 34、滤光片 22、收集器 44、灯泡 30、激光 46、辐射源 50、寒冰 52、火焰 52、加速器 82、光闸 20、传感器 28、电容 68。
- 保留现有关卡容量和起始金币；击杀功率奖励为普通/快速 1W、重甲/抗性 2W、Boss 20W。
- 19 关保留现有敌人数量、路线和主题，血量统一由以下规则生成：
  - 基础普通 HP：`26 + 7 × 关卡编号`。
  - 快速 ×0.78、重甲 ×2.8、抗性 ×1.65、Boss ×7.5。
  - 波次倍率依次为 0.82、0.94、1.06、1.18、1.30、1.42。
  - 关卡段倍率：1–3 关 ×0.65，4–10 关 ×0.85，11–19 关 ×1.0。
- 解锁教学顺序：L1 基础光路、L2 激光、L3 光闸、L4 分束/辐射、L5 RGB 合束/火焰、L6 棱镜分束/滤光、L7 寒冰/收集器、L8 传感器、L9 开放加速器、电容及完整仪器组。
- 难度验收代理目标：
  - L1–3 推荐阵容结束时核心不低于 80%。
  - L4–10 推荐阵容核心不低于 60%，忽略本关机制的单色阵容应明显泄漏。
  - L11–19 推荐阵容核心不低于 25%，无状态组合或无抗性应对的阵容应失败。
  - 状态目标平均存活至少 3 秒，使附着、护盾、抗性和反应视觉可被观察。

## 引导与视觉

- 首次进入自动将光路塔防组件滚动到可操作区域，并显示贴边、可关闭的教学条，不使用全屏欢迎卡。
- L1 实时检测并勾选：选择红光源、放置光源、放置镜面、放置灯泡、吸附镜面输出、启动波次、观察首次击杀；推荐孔位高亮但不锁定其他操作。
- L3–L9 首次进入分别显示光闸、分束、混色反应、棱镜色散、抗性/收集、传感器、白光破盾与高级终端的短任务；可跳过并从手册重播。
- 游戏说明改为“快速上手、颜色与反应、光路仪器、攻击终端、敌人、数值”六个页签，列出真实倍率、持续时间、护盾和抗性规则。
- Phaser 增加棱镜三角形器件、RGB 分离光束、寒冰/火焰脉冲环、加速器贯穿轨迹、护盾刻度、白光破盾裂纹、易伤和反应反馈。
- 设备面板显示实际输入、传输损耗、输出功率、攻击周期、状态效果与收集器颜色；陌生图标提供悬停提示。
- 保持现有冷色实验室视觉方向，控制面板继续使用紧凑直角布局；桌面和横屏触控下不得遮挡 Canvas 或出现文字溢出。

## 验证与交付

- 单元测试覆盖棱镜两种模式、三路独立吸附、底层分束归一化、光路守恒、收集器颜色与扣减、固定步长一致性、反应冷却、辐射衰减、护盾/白光易伤、冰火周期脉冲、加速器直线穿透和 v3 存档迁移。
- 增加 19 关确定性推荐阵容基准和错误阵容对照，记录泄漏、首次击杀时间、状态触发数、每瓦伤害和每金币伤害。
- 运行 `typecheck`、`lint`、`build`、光路塔防单元测试及 Playwright E2E；数学与模拟使用自动测试，最终体验以浏览器人工试玩为主。
- 人工核查 1920×1080、1366×768、844×390 横屏触控：完成 L1 教学、L6 棱镜色散、L9 白光破盾、后期冰火脉冲和加速器贯穿，并保存关键状态截图进行前后比对。
- 更新数值审计探针和报告，确保刷新率伤害误差低于 0.5%、倍速时间保留率为 100%、任何光路总交付不超过输入。
- 将最终改动、验证结果、截图位置和剩余风险追加到现有 `PROGRESS.md`；不改动无关的博客文件，不提交、推送或部署。
- 实现预计不需要 Python；如确需脚本，只使用 `D:\Anaconda3\envs\selfcontrol\python.exe`。
